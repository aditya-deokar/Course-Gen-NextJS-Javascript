const {
    GoogleGenerativeAI,
    HarmCategory,
    HarmBlockThreshold,
  } = require("@google/generative-ai");
  
  const apiKey = process.env.NEXT_PUBLIC_GEMINI_API_KEY;
  const genAI = new GoogleGenerativeAI(apiKey);
  
  const model = genAI.getGenerativeModel({
    model: "gemini-2.0-flash",
  });
  
  const generationConfig = {
    temperature: 1,
    topP: 0.95,
    topK: 40,
    maxOutputTokens: 8192,
    responseMimeType: "application/json",
  };
  

  export const GenerateCourseLayout_AI = model.startChat({
      generationConfig,
      history: [
        {
          role: "user",
          parts: [
            {text: "Generate A Course Tutorial on following details with field as CourseName, Description, Along with ChapterName, About, duration:  category: \"Programming\" ,Topic: Python, level: basic, Duration: 1 hours , NoOfChapters: 5, in JSON format"},
          ],
        },
        {
          role: "model",
          parts: [
            {text: "```json\n{\n  \"CourseName\": \"Python Programming: A Beginner's Guide\",\n  \"Description\": \"This course provides a foundational understanding of Python programming, suitable for individuals with no prior programming experience. You'll learn the basics of Python syntax, data structures, control flow, and object-oriented programming, enabling you to write simple programs and solve practical problems.\",\n  \"Category\": \"Programming\",\n  \"Topic\": \"Python\",\n  \"Level\": \"Basic\",\n  \"Duration\": \"1 hour\",\n  \"NoOfChapters\": 5,\n  \"Chapters\": [\n    {\n      \"ChapterName\": \"Chapter 1: Introduction to Python\",\n      \"About\": \"This chapter introduces you to Python, its history, advantages, and its diverse applications. We'll guide you through the installation process and set up your development environment.  We'll also cover the difference between Python 2 and 3, emphasizing the best practices for starting with Python 3.\",\n      \"Duration\": \"12 minutes\"\n    },\n    {\n      \"ChapterName\": \"Chapter 2: Data Types and Variables\",\n      \"About\": \"Learn about fundamental data types in Python, including integers, floats, strings, and booleans. You'll discover how to declare variables, assign values, and perform basic arithmetic operations.  We will also cover variable naming conventions and the importance of comments in your code.\",\n      \"Duration\": \"12 minutes\"\n    },\n    {\n      \"ChapterName\": \"Chapter 3: Control Flow (Conditional Statements)\",\n      \"About\": \"This chapter focuses on control flow statements like `if`, `elif`, and `else`. You'll learn how to make decisions in your code based on conditions and implement logic for different scenarios. We'll explore comparison operators and logical operators.\",\n      \"Duration\": \"12 minutes\"\n    },\n    {\n      \"ChapterName\": \"Chapter 4: Loops (For and While)\",\n      \"About\": \"Master the concept of loops using `for` and `while` statements.  You'll learn how to iterate over sequences of data, repeat code blocks, and create powerful algorithms. We'll also cover `break` and `continue` statements for controlling loop execution.\",\n      \"Duration\": \"12 minutes\"\n    },\n    {\n      \"ChapterName\": \"Chapter 5: Functions\",\n      \"About\": \"Discover the power of functions for code organization and reusability. You'll learn how to define functions, pass arguments, return values, and understand the concept of scope. We'll also explore built-in Python functions and introduce the basics of importing modules.\",\n      \"Duration\": \"12 minutes\"\n    }\n  ]\n}\n```"},
          ],
        },
      ],
    });
  
    // const result = await chatSession.sendMessage("INSERT_INPUT_HERE");
    // console.log(result.response.text());


    export const GenerateChapterContent_AI = model.startChat({
      generationConfig,
      history: [
        {
          role: "user",
          parts: [
            {text: "Explain the concept in detail on Topic: javascript, specific in Chapter:Chapter 2: Prototypes and Inheritance cover all the chapter points like Explore JavaScript's prototypal inheritance model. Learn how objects inherit properties and methods from prototypes and how to create custom inheritance chains. This chapter covers `__proto__`, `prototype`, and `Object.create()`. and add more , in JSON Format with list of array with field as title, description in detail, code example (Code field in <precode> format) if applicable and ensure the reading material must in 40 minutes"},
          ],
        },
        {
          role: "model",
          parts: [
            {text: "```json\n[\n  {\n    \"title\": \"Introduction to Prototypes and Inheritance in JavaScript\",\n    \"description\": \"JavaScript employs a prototypal inheritance model, a fundamental concept that differs significantly from classical inheritance found in languages like Java or C++. In JavaScript, objects inherit properties and methods directly from other objects, called prototypes. Understanding this mechanism is crucial for building robust and maintainable applications.\",\n    \"code_example\": null\n  },\n  {\n    \"title\": \"What is a Prototype?\",\n    \"description\": \"Every object in JavaScript has a prototype.  The prototype is itself an object, and it's from this object that the original object inherits properties and methods.  Think of it as a blueprint or a template. When you try to access a property or method of an object, JavaScript first looks for it directly on the object. If it's not found there, it continues searching up the prototype chain.\",\n    \"code_example\": null\n  },\n  {\n    \"title\": \"The `__proto__` Property\",\n    \"description\": \"The `__proto__` property (dunder proto) is a property of every object that points to its prototype. While widely implemented, it's considered non-standard. Its primary use is for inspecting and sometimes modifying the prototype chain, but direct manipulation is discouraged in favor of more standard approaches like `Object.getPrototypeOf` and `Object.setPrototypeOf` or `Object.create()`. Direct modification of `__proto__` can impact performance due to optimization issues in JavaScript engines.\",\n    \"code_example\": \"<pre>const myObject = {};\\n\\n// Get the prototype of myObject using __proto__\\nconst prototype = myObject.__proto__;\\n\\nconsole.log(prototype); // Output: {} (Object.prototype)\\n</pre>\"\n  },\n  {\n    \"title\": \"The `prototype` Property\",\n    \"description\": \"The `prototype` property is a property of functions. It’s the object that will be assigned as the prototype to new objects created by that function when used as a constructor (using the `new` keyword).  This is the key mechanism for setting up inheritance in JavaScript. Non-function objects do not have a `prototype` property; they use `__proto__` (or `Object.getPrototypeOf`) to access their prototype.\",\n    \"code_example\": \"<pre>function Person(name) {\\n  this.name = name;\\n}\\n\\nPerson.prototype.greet = function() {\\n  return `Hello, my name is ${this.name}`;\\n};\\n\\nconst john = new Person('John');\\nconsole.log(john.greet()); // Output: Hello, my name is John\\n\\nconsole.log(Person.prototype); // The object assigned to john.__proto__\\n</pre>\"\n  },\n  {\n    \"title\": \"Creating Objects with `new` (Constructor Functions)\",\n    \"description\": \"When you use the `new` keyword with a function, it performs the following steps:\\n\\n1.  A new, empty object is created.\\n2.  The `this` keyword is set to point to the new object.\\n3.  The `__proto__` property of the new object is set to the function's `prototype` property.\\n4.  The function is executed, using the new object as the context (i.e., `this`).\\n5.  If the function doesn't return anything explicitly (or returns a non-object), the new object is returned.\",\n    \"code_example\": \"<pre>function Animal(name) {\\n  this.name = name;\\n}\\n\\nAnimal.prototype.sayName = function() {\\n  console.log(`My name is ${this.name}`);\\n};\\n\\nconst dog = new Animal('Buddy');\\ndog.sayName(); // Output: My name is Buddy\\n\\nconsole.log(dog.__proto__ === Animal.prototype); // Output: true\\n</pre>\"\n  },\n  {\n    \"title\": \"The Prototype Chain\",\n    \"description\": \"When you try to access a property or method on an object, JavaScript searches for it in the following order:\\n\\n1.  On the object itself.\\n2.  On the object's prototype (`__proto__`).\\n3.  On the prototype of the object's prototype, and so on, until it reaches `Object.prototype`.\\n4.  If the property or method is still not found, and you've reached the end of the chain (`Object.prototype.__proto__` is `null`), JavaScript returns `undefined`.  This chain of prototypes is called the prototype chain.\",\n    \"code_example\": \"<pre>function Vehicle(make) {\\n  this.make = make;\\n}\\n\\nVehicle.prototype.startEngine = function() {\\n  console.log('Engine starting...');\\n};\\n\\nfunction Car(make, model) {\\n  Vehicle.call(this, make); // Call the parent constructor\\n  this.model = model;\\n}\\n\\n// Set Car.prototype to inherit from Vehicle.prototype\\nCar.prototype = Object.create(Vehicle.prototype);\\nCar.prototype.constructor = Car; // Reset the constructor property\\n\\nconst myCar = new Car('Toyota', 'Camry');\\nmyCar.startEngine(); // Output: Engine starting...\\nconsole.log(myCar.make); // Output: Toyota\\nconsole.log(myCar.model); // Output: Camry\\n\\nconsole.log(myCar.__proto__.__proto__ === Vehicle.prototype); // Output: true \\nconsole.log(myCar.__proto__.__proto__.__proto__ === Object.prototype); // Output: true\\n</pre>\"\n  },\n  {\n    \"title\": \"`Object.create()`\",\n    \"description\": \"`Object.create()` is a standard and recommended way to create new objects with a specific prototype.  It allows you to create an object with its `__proto__` set to any other object you provide. This is crucial for establishing inheritance without relying on constructor functions and the `new` keyword.  It takes one required argument: the object that should be the prototype of the newly created object.  An optional second argument can specify properties to add to the newly created object.\",\n    \"code_example\": \"<pre>const animal = {\\n  type: 'animal',\\n  makeSound: function() {\\n    console.log('Generic animal sound');\\n  }\\n};\\n\\nconst dog = Object.create(animal);\\ndog.breed = 'Labrador';\\ndog.makeSound = function() {\\n  console.log('Woof!');\\n};\\n\\ndog.makeSound(); // Output: Woof!\\nconsole.log(dog.type); // Output: animal\\n\\nconst cat = Object.create(animal, {\\n    breed: { value: 'Siamese', writable: true, enumerable: true, configurable: true }\\n});\\n\\nconsole.log(cat.breed); // Output: Siamese\\n</pre>\"\n  },\n  {\n    \"title\": \"Benefits of Prototypal Inheritance\",\n    \"description\": \"Prototypal inheritance offers several advantages:\\n\\n*   **Code Reusability:**  Methods and properties defined on a prototype are shared by all objects that inherit from it, reducing code duplication.\\n*   **Dynamic Inheritance:** You can modify the prototype at runtime, and all objects that inherit from it will immediately reflect the changes (though be cautious about doing this in production).\\n*   **Flexibility:** Prototypal inheritance is very flexible and allows for a variety of inheritance patterns.\",\n    \"code_example\": null\n  },\n  {\n    \"title\": \"Differences from Classical Inheritance\",\n    \"description\": \"Unlike classical inheritance where classes inherit from other classes, prototypal inheritance involves objects inheriting directly from other objects (prototypes). This can lead to more flexible and dynamic systems. Classical inheritance often involves a rigid class hierarchy, whereas prototypal inheritance allows for more fluid relationships between objects.\",\n    \"code_example\": null\n  },\n  {\n    \"title\": \"Overriding Properties and Methods\",\n    \"description\": \"When a property or method is defined on an object that already exists on its prototype, the object's own property or method takes precedence. This is called *shadowing* or *overriding*.  JavaScript will first look for the property on the object itself before traversing up the prototype chain.\",\n    \"code_example\": \"<pre>const person = {\\n  greeting: 'Hello'\\n};\\n\\nconst employee = Object.create(person);\\nemployee.greeting = 'Welcome'; // Overrides the greeting property\\n\\nconsole.log(employee.greeting); // Output: Welcome\\nconsole.log(person.greeting); // Output: Hello\\n</pre>\"\n  },\n  {\n    \"title\": \"Checking for Properties: `hasOwnProperty()`\",\n    \"description\": \"The `hasOwnProperty()` method, inherited from `Object.prototype`, allows you to check if an object has a specific property defined directly on itself, as opposed to inheriting it from its prototype. This is important for distinguishing between own properties and inherited properties.\",\n    \"code_example\": \"<pre>const obj = {\\n  ownProperty: 'This is mine'\\n};\\n\\nconst inheritedObj = Object.create(obj);\\ninheritedObj.newProperty = 'I am a new property';\\n\\nconsole.log(inheritedObj.hasOwnProperty('newProperty')); // Output: true\\nconsole.log(inheritedObj.hasOwnProperty('ownProperty'));   // Output: false\\nconsole.log(obj.hasOwnProperty('ownProperty'));            // Output: true\\n</pre>\"\n  },\n  {\n    \"title\": \"Pitfalls and Best Practices\",\n    \"description\": \"While prototypal inheritance is powerful, it can also be a source of confusion.  Avoid direct manipulation of `__proto__` whenever possible.  Use `Object.create()` to establish inheritance chains.  Always remember that changes to a prototype affect all objects that inherit from it.  Be mindful of the prototype chain's length to avoid performance issues when accessing properties.\",\n    \"code_example\": null\n  },\n  {\n    \"title\": \"Conclusion\",\n    \"description\": \"Understanding JavaScript's prototypal inheritance is essential for becoming a proficient JavaScript developer. By leveraging prototypes, you can create efficient, reusable, and maintainable code. Mastering `__proto__`, `prototype`, and `Object.create()` gives you the tools to build complex object hierarchies and effectively share functionality across your application.\",\n    \"code_example\": null\n  }\n]\n```"},
          ],
        },
      ],
    });